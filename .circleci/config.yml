version: 2.1
jobs:
  spuyo:
    docker:
      - image: spuyo/spuyo-ci
    steps:
      - checkout
      - run:
          name: Hello
          command: echo "Hello spuyo" && ls
workflows:
  spuyo:
    jobs:
      - spuyo